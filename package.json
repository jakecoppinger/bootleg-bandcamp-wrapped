{
  "name": "bandcamp-wrapped",
  "version": "1.0.0",
  "description": "Bandcamp Wrapped - Spotify Wrapped but for Bandcamp",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test:watch": " ls src/* src/**/* | entr -rcs 'npm run test'",
    "build": "npm run typecheck && esbuild src/index.ts --bundle --format=esm --target=es2020 --outfile=dist/index.js && cp src/index.html dist/index.html",
    "dev": "cp src/index.html dist/index.html && esbuild src/index.ts --bundle --format=esm --target=es2020 --outfile=dist/index.js --serve=8000 --servedir=dist",
    "serve": "npx http-server dist/ -p 8000",
    "test": "jest"
  },
  "devDependencies": {
    "@types/jest": "^30.0.0",
    "@types/node": "^22.0.0",
    "esbuild": "^0.27.2",
    "jest": "^30.2.0",
    "jest-environment-jsdom": "^30.2.0",
    "ts-jest": "^29.4.6",
    "typescript": "^5.7.0"
  }
}
